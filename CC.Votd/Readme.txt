General notes:

- Make sure to update the version in the AssemblyInfo.cs file *AND* the NativeResources.res
- Only update the installer version / product code immediately prior to releasing a new version